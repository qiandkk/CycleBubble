[pytest]
# 极简 pytest 配置 —— 避免和 backend/ 业务代码的日志/环境冲突
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 抑制 import 阶段 backend.* 缺 jwt_secret 的报错（conftest 已注入 CB_JWT_SECRET）
filterwarnings =
    ignore::DeprecationWarning
